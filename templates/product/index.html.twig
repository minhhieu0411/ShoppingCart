{% extends "base.html.twig" %}

{% block body %}
<div class="container col-md-8 text-center mt-3">
<table class="table table-light">
    <tr>
        <th colspan="3">
            <h1 class="text text-primary">Product List</h1>
        </th>
        <th>
           
            </a>
        </th>
    </tr>
    <tr>
                <th>Id</th>
                <th>Product Name</th>
                <th>Product Description</th>
                <th>keyword</th>
                <th>Product Image</th>
                <th>Product Price</th>
                <th>Date</th>
                <th>Status</th>
                <th>Actions</th>
    </tr>
    {% for product in products %}
        <tr>
            <td>{{ product.id }}</td>
                <td>{{ product.productTitle }}</td>
                <td>{{ product.productDescription }}</td>
                <td>{{ product.productKeyword }}</td>
                <td><img src= {{product.productImage}} alt="" class="card-img-center" width="50" height="50"></td>
                <td>{{ product.productPrice }}</td>
                <td>{{ product.date ? product.date|date('d-m-Y') : '' }}</td>
                <td>{{ product.status }}</td>
            <td>
              <td>
              <a href="{{ path('product_add', {'id': product.id}) }}">New</a>
                    <a href="{{ path('product_detail', {'id': product.id}) }}">Detail</a>
                    <a href="{{ path('product_update', {'id': product.id}) }}">Update</a>
                </td>
               <button><a class="text-decoration-none" href="{{ path('product_delete', {'id' : product.id }) }}"
                onclick="return confirm('Are you sure to delete this author ?');"</button>
                >
                 
                </a>
            </td>
        </tr>
    {% endfor %}
</table>
</div>
{% endblock %}